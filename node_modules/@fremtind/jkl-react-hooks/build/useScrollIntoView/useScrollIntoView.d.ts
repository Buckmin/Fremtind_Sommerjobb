import { RefObject } from "react";
declare type ValueOf<T> = T[keyof T];
interface Props {
    ref: RefObject<ValueOf<HTMLElementTagNameMap>>;
    timeout?: number;
    autoScroll?: boolean;
    options?: ScrollIntoViewOptions;
}
export declare const useScrollIntoView: ({ ref, timeout, autoScroll, options }: Props) => (() => void)[];
export {};
