import { ChangeEventHandler } from "react";
import { LabelVariant, ValuePair } from "@fremtind/jkl-core";
interface Props {
    name: string;
    legend: string;
    choices: Array<string | ValuePair>;
    selectedValue?: string;
    inline?: boolean;
    onChange: ChangeEventHandler<HTMLInputElement>;
    helpLabel?: string;
    errorLabel?: string;
    variant?: LabelVariant;
    forceCompact?: boolean;
    className?: string;
    inverted?: boolean;
}
export declare const RadioButtons: ({ name, legend, choices, selectedValue, onChange, inline, helpLabel, errorLabel, variant, forceCompact, className, inverted, }: Props) => JSX.Element;
export {};
