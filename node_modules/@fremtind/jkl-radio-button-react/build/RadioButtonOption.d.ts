import { ChangeEventHandler } from "react";
interface Props {
    value: string;
    label: string;
    name: string;
    onChange: ChangeEventHandler<HTMLInputElement>;
    inline?: boolean;
    checked?: boolean;
    invalid?: boolean;
    forceCompact?: boolean;
    inverted?: boolean;
}
export declare const RadioButtonOption: ({ value, label, onChange, name, inline, checked, invalid, forceCompact, inverted, }: Props) => JSX.Element;
export {};
