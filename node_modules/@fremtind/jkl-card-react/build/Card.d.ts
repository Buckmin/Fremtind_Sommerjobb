import { ReactNode, MouseEventHandler } from "react";
declare type validButtons = "primary" | "secondary" | "tertiary";
declare type Action = {
    type: validButtons;
    name: string;
    onClick: MouseEventHandler<HTMLButtonElement>;
};
declare type Media = {
    src: string;
    alt: string;
};
declare type Clickable = {
    href?: string;
    onClick?: MouseEventHandler<HTMLAnchorElement>;
};
interface Props {
    title?: string;
    children?: ReactNode;
    className?: string;
    media?: Media;
    action?: Action;
    dark?: boolean;
    clickable?: Clickable;
}
export declare const Card: ({ title, children, className, media, action, dark, clickable }: Props) => JSX.Element;
export {};
