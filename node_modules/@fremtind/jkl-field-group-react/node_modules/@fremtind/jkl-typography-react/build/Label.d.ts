import { ReactNode } from "react";
import { LabelVariant } from "@fremtind/jkl-core";
interface Props {
    variant?: LabelVariant;
    forceCompact?: boolean;
    srOnly?: boolean;
    children: ReactNode;
    standAlone?: boolean;
    htmlFor?: string;
}
export declare function Label({ variant, forceCompact, srOnly, children, standAlone, htmlFor }: Props): JSX.Element;
export {};
