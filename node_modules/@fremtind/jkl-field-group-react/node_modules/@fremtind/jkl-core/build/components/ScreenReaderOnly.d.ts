import { ReactNode } from "react";
interface Props {
    children: ReactNode;
    showOnFocus?: boolean;
}
export declare const ScreenReaderOnly: ({ children, showOnFocus }: Props) => JSX.Element;
export default ScreenReaderOnly;
