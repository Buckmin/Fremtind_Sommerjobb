import { ReactNode } from "react";
import { LabelVariant } from "@fremtind/jkl-core";
interface Props {
    legend: string;
    children?: ReactNode;
    className?: string;
    helpLabel?: string;
    errorLabel?: string;
    variant?: LabelVariant;
    forceCompact?: boolean;
}
export declare const FieldGroup: ({ legend, className, children, helpLabel, errorLabel, variant, forceCompact, }: Props) => JSX.Element;
export {};
